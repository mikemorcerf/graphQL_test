type Todo {
  id: ID!
  description: String!
  checked: Boolean!
}

type Query {
  todos: [Todo!]!
  todo(id: ID!): Todo
}

type Mutation {
  createTodo(description: String!): Todo!
  checkTodo(id: ID!): Boolean
  updateTodo(description: String!): Todo!
  deleteTodo(id: ID!): Boolean
}